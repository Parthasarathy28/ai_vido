# --- Script writing (pick one; auto-detection prefers OpenAI, then Anthropic) ---
OPENAI_API_KEY=        # gpt-4o-mini scripts + DALL-E 3 images
ANTHROPIC_API_KEY=     # Claude scripts (alternative to OpenAI)

# --- Images (optional; auto order: GPU SD > DALL-E (OpenAI) > Pexels > free Pollinations) ---
PEXELS_API_KEY=        # free real stock photos

# --- Voice (optional; needed for Tamil/Kannada narration) ---
ELEVENLABS_API_KEY=    # multilingual voice incl. Tamil/Kannada
ELEVENLABS_VOICE_ID=   # optional, defaults to a standard voice
