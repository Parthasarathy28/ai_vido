# Web-boot dependencies only. Kept lean so the FastAPI app fits inside
# Vercel's 250MB serverless bundle. The pipeline's heavier deps
# (anthropic, openai, piper-tts, faster-whisper, Pillow, requests,
# google-api-python-client) live in requirements-pipeline.txt and are
# lazy-imported only by the stage that needs them — so the dashboard
# boots fine without them; video generation needs the full install.
fastapi>=0.110
pydantic>=2.5
PyYAML>=6.0
python-dotenv>=1.0
python-multipart>=0.0.9
rich>=13.0
typer>=0.12
uvicorn[standard]>=0.27
